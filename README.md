## Overview
ANARI Project. In progress.

## Requirements
- vcpkg installed
- CMake 3.19
- C++20 supported

## Build
Follow the instructions:
```bash
cmake --preset=default
cmake -B build -S ./
cmake --build build
```
